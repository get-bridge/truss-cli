package cmd

import (
	"testing"

	. "github.com/smartystreets/goconvey/convey"
)

func TestSecretsInit(t *testing.T) {
	Convey("secrets init", t, func() {
		// TODO how can we mock init?
		Convey("runs with no errors", nil)
	})
}
