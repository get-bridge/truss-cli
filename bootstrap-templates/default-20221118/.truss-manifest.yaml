name: default-20221118
description: It's the newest
version: 0.1.0
params:
  - name: name
    type: string
    prompt: What's the name of your app?
  - name: role
    type: string
    prompt: What's the role of your service? (i.e. api, web)
  - name: httpPort
    type: string
    prompt: What is your app's HTTP port?
  - name: healthCheckPath
    type: string
    prompt: What is your app's health check path? (i.e. /actuator/health)
  - name: image
    type: string
    prompt: What is the name of your Docker image? Don't include any tags (i.e. 127178877223.dkr.ecr.us-east-2.amazonaws.com/my-app)
  - name: slack_channel
    type: string
    prompt: What Slack channel do you want to use for Spinnaker notifications (i.e. #my-app-alerts)
